import React from "react";

function SupportTabContent({ children }) {
    return (
        <div className="tab-content support_tab_content">{children}</div>
    )
}

export default SupportTabContent;
